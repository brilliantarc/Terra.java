package brilliantarc.terra;

import brilliantarc.terra.node.Heading;
import brilliantarc.terra.node.Taxonomy;
import com.google.common.base.Predicate;
import com.google.common.collect.Iterables;
import org.junit.Test;
import java.util.List;
import static org.junit.Assert.*;
import static brilliantarc.terra.DefaultSettings.*;

/**
 * Superheadings and headings are tested in this class.
 */
public class HeadingTests {

    @Test
    public void allHeadings() {
//        List<Heading> headings = terra.headings().all("MCZ");
//        assertNotNull(headings);
//        assertTrue(headings.size() > 0);
//        System.out.println("First is " + headings.get(0).getName());
//        assertTrue(Iterables.all(taxonomies, new Predicate<Taxonomy>() {
//            public boolean apply(Taxonomy taxonomy) {
//                return TEST_PORTFOLIO.equals(taxonomy.getOpco());
//            }
//        }));
    }

}
