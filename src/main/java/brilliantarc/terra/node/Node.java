package brilliantarc.terra.node;

/**
 * Each type of node implements this interface.  Right now it doesn't provide
 * for any functionality, except allowing mixed lists of results, from things
 * like search.
 */
public interface Node {
}
